# AI Challenge 2025
# Deepfake Detection Web Application

## 📋 Описание

Приложение использует дообученную модель Vision Transformer (ViT-Base с предобучением CLIP) для определения подлинности лиц на изображениях и в видео. Система автоматически обнаруживает лица, анализирует их и выдает вердикт: оригинал или дипфейк.

## ✨ Основные возможности

- 🔍 **Автоматическое обнаружение лиц** с помощью MediaPipe
- 📸 **Анализ изображений** - обнаружение и классификация всех лиц на изображении
- 🎬 **Анализ видео** - обработка видео с выборкой кадров и временной шкалой вероятности
- 📊 **Визуализация результатов** - график вероятности по времени для видео
- 🎨 **Теплокарты (Grad-CAM)** - визуализация областей внимания модели для дипфейков
- 🔗 **Загрузка по URL** - возможность загружать файлы по прямой ссылке
- 📋 **Вставка из буфера обмена** - быстрая вставка изображений через Ctrl+V
- 🎛️ **Настройка порога уверенности** - регулировка чувствительности детекции (0.3-0.7)
- ⚡ **Режимы обработки видео** - быстрый (каждый 5-й кадр) или точный (все кадры)
- 🎨 **Современный интерфейс** - адаптивный дизайн с темной темой

## 🚀 Установка и запуск

### Требования

- Python 3.8 или выше
- CUDA (опционально, для использования GPU)

### Вариант 1: Для организаторов (ZIP архив)

Если у вас есть ZIP архив с проектом:

1. **Распакуйте архив** в удобную папку
2. **Откройте терминал/командную строку** в папке проекта
3. Перейдите к разделу **"Создание виртуального окружения"** ниже

### Вариант 2: Для разработчиков (Git)

#### Убедитесь, что установлен Git LFS (это нужно для загрузки весов моделей):  

```bash
git lfs install
```

#### Клонируйте репозиторий:  

```bash
git clone https://github.com/k0rshz/deepfake-detection.git
cd deepfake_detection
```

#### Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Подготовка модели

Убедитесь, что файл модели `models/best_model.pth` находится в папке `models/`. 

#### Запуск приложения

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Для Windows** (если uvicorn не найден):
```bash
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### Открытие в браузере

Откройте браузер и перейдите по адресу:
```
http://localhost:8000
```

## 📁 Структура проекта

```
deepfake_detection/
├── main.py                 # Основной файл приложения (FastAPI)
├── models/
│   ├── model.py           # Модель ImageClassifier (PyTorch Lightning)
│   └── best_model.pth     # Обученная модель (должна быть добавлена)
├── examples/              # Примеры для тестирования
│   ├── image/             # Примеры изображений
│   │   └── ...
│   └── video/             # Примеры видео
│       └── ...
├── templates/
│   └── index.html         # HTML шаблон
├── static/
│   ├── css/
│   │   └── style.css      # Стили интерфейса
│   ├── js/
│   │   └── main.js        # JavaScript логика
│   └── uploads/           # Папка для загруженных файлов (создается автоматически)
├── requirements.txt       # Зависимости проекта
├── .gitignore            # Игнорируемые файлы Git
└── README.md             # Документация
```

## 🎯 Использование

### Анализ изображения

1. Выберите способ загрузки: **📁 Загрузка файла** или **🔗 Загрузка по URL**
2. Если загружаете файл:
   - Выберите тип файла: **Изображение**
   - Загрузите изображение (JPEG, PNG) через кнопку или перетащите в область
   - **Или вставьте изображение из буфера обмена** (Ctrl+V)
3. Настройте **Порог уверенности** (опционально, по умолчанию 0.50)
4. Нажмите **Анализировать**
5. Просмотрите результаты:
   - Аннотированное изображение с выделенными лицами
   - Для каждого лица: вердикт (Оригинал/Дипфейк) и вероятность
   - Для дипфейков: теплокарта с областями внимания модели

### Анализ видео

1. Выберите способ загрузки: **📁 Загрузка файла** или **🔗 Загрузка по URL**
2. Выберите тип файла: **Видео**
3. Настройте параметры:
   - **Количество кадров для вывода** (5-25) - сколько кадров с лицами будет показано
   - **Режим скорости**:
     - ⚡ **Быстро** - обрабатывается каждый 5-й кадр (быстрее, но менее точно)
     - 🎯 **Точно** - обрабатываются все кадры (медленнее, но максимально точно)
   - **Порог уверенности** (опционально, по умолчанию 0.50)
4. Загрузите видео (MP4, AVI, MOV)
5. Нажмите **Анализировать**
6. Просмотрите результаты:
   - Общую статистику по видео с визуальным индикатором статуса
   - График вероятности по времени (временная шкала)
   - Обнаруженные лица с вердиктами и вероятностями

### Загрузка по URL

1. Переключитесь на вкладку **🔗 Загрузка по URL**
2. Введите прямую ссылку на изображение или видео
3. Выберите тип файла (Изображение/Видео)
4. Настройте параметры (как при обычной загрузке)
5. Нажмите **Анализировать по URL**

**Важно:** URL должен быть прямой ссылкой на файл (например, `https://example.com/image.jpg`), а не на HTML-страницу.

## 📊 Особенности интерфейса

### Визуальные улучшения

- **Превью файлов** - компактное окно предпросмотра (максимум 600px) с автоматическим центрированием
- **Вставка из буфера обмена** - просто нажмите Ctrl+V для быстрой вставки изображения
- **Визуальные индикаторы статуса** - цветные бейджи (✓ Оригинал / ✗ Дипфейк) для быстрой идентификации
- **Теплокарты** - показываются только для дипфейков, визуализируют области внимания модели
- **Модальное окно** - увеличенный просмотр изображений и результатов по клику
- **Анимации** - плавные переходы и эффекты
- **Информационная панель** - развернутая информация о функциях приложения

### Настройки

- **Порог уверенности** (0.3-0.7):
  - Определяет, при какой вероятности лицо считается оригиналом или дипфейком
  - Вероятность выше порога = Оригинал
  - Вероятность ниже порога = Дипфейк
  - По умолчанию: 0.50

- **Режимы обработки видео**:
  - **⚡ Быстро**: обрабатывается каждый 5-й кадр (быстрее, но чуть менее точно)
  - **🎯 Точно**: обрабатываются все кадры (медленнее, но более точно)

### Валидация

- **Размер файла**: максимум 100 МБ
- **Форматы изображений**: JPEG, PNG
- **Форматы видео**: MP4, AVI, MOV
- **Минимальный размер лица**: 220 пикселей

## 🔧 Технические детали

### Модель

- **Архитектура**: Vision Transformer (ViT) из библиотеки `timm`
- **Базовая модель**: `vit_base_patch16_clip_224.openai`
- **Фреймворк**: PyTorch Lightning
- **Визуализация**: Grad-CAM для теплокарт

### Детектор лиц

- **Библиотека**: MediaPipe Face Detection
- **Минимальный размер лица**: 220 пикселей
- **Паддинг**: 50% от размера обнаруженного лица

### Обработка видео

- **Пропуск кадров**: настраивается через режим скорости (1 или 5)
- **Выборка кадров**: равномерная выборка для вывода (5-25 кадров)
- **Временная шкала**: график вероятности по времени с отметками дипфейков
- **Агрегация результатов**: итоговая вероятность для видео рассчитывается как **среднее арифметическое** всех вероятностей по всем обработанным кадрам с обнаруженными лицами

### Backend

- **Фреймворк**: FastAPI
- **Асинхронная обработка**: поддержка асинхронных запросов
- **Валидация**: проверка типов файлов и размеров

## 🐛 Решение проблем

### Проблема: Модель не загружается

**Решение**: 
1. Убедитесь, что файл `models/best_model.pth` существует
2. Проверьте путь к модели в `main.py` (переменная `MODEL_PATH`)
3. Убедитесь, что у вас достаточно памяти для загрузки модели

### Проблема: Ошибка при обработке видео

**Решение**: 
1. Проверьте формат видео (MP4, AVI, MOV)
2. Убедитесь, что размер не превышает 100 МБ
3. Проверьте, что видео не повреждено
4. Попробуйте другой режим скорости

### Проблема: Лица не обнаружены

**Решение**: 
1. Убедитесь, что на изображении/видео есть лица
2. Лица должны быть достаточно крупными (минимум 220 пикселей)
3. Проверьте качество изображения/видео
4. Убедитесь, что лица хорошо видны и не закрыты

### Проблема: Ошибка при загрузке по URL

**Решение**:
1. Убедитесь, что URL является прямой ссылкой на файл
2. Проверьте, что файл доступен (не требует авторизации)
3. Убедитесь, что формат файла поддерживается
4. Проверьте размер файла (максимум 100 МБ)

### Проблема: Приложение не запускается

**Решение**:
1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
3. Убедитесь, что порт 8000 свободен
4. Проверьте логи ошибок в консоли

### Проблема: Теплокарты не отображаются

**Решение**:
1. Теплокарты показываются только для дипфейков
2. Убедитесь, что модель правильно определила дипфейк
3. Проверьте консоль браузера на наличие ошибок

## 📝 Примечания

- Приложение создано в образовательных целях
- Точность детекции зависит от качества модели и входных данных
- Для лучших результатов используйте качественные изображения/видео с хорошо видимыми лицами
- Режим "Точно" для видео может занять значительно больше времени

## 📦 Примеры для тестирования

В папке `examples/` находятся примеры изображений и видео для тестирования приложения:

- **`examples/image/`** - содержит примеры изображений:
  - `real1.jpg`, `real2.jpg` - оригинальные изображения
  - `deepfake1.jpg`, `deepfake2.png` - дипфейк изображения
  
- **`examples/video/`** - содержит примеры видео:
  - `real1.mp4`, `real2.mp4` - оригинальные видео
  - `deepfake1.mp4`, `deepfake2.mp4` - дипфейк видео

Вы можете использовать эти файлы для проверки работы приложения перед загрузкой собственных данных.

## 🔮 Реализованные функции

- ✅ Автоматическое обнаружение лиц
- ✅ Анализ изображений и видео
- ✅ Визуализация внимания модели (Grad-CAM / теплокарты)
- ✅ Загрузка файлов по URL
- ✅ Вставка из буфера обмена
- ✅ Настройка порога уверенности
- ✅ Режимы скорости обработки видео
- ✅ Визуальные индикаторы статуса
- ✅ Временная шкала вероятности для видео
- ✅ Модальное окно для просмотра результатов
- ✅ Адаптивный интерфейс

